# Login

- Every user must login to the Signature in order to use the functions in the signature.

> Enter registered valid user credentials and click "LOGIN"

<img src="../../admin/login.png" />

> Login Successful - Dashboard Page

<img src="../../admin/dashboard.png" />

> Login Failure Page

<img src="../../admin/login-faliure.png" />

### REST API

<dl>
  <dd>Name: **Login**</dd>
  <dd>Method: **POST**</dd>
  <dd>Headers: </dd>
</dl>
<dl>
  <dd>Body:</dd>
  ```javascript {3} copy
{
  "variables": {
    "input": {
      "username": "vellies@yopmail.com",
      "password": "Admin@123"
    }
  },
  "query": "query ($input: LoginRequest) {\n  login(input: $input) {\n    responseCode\n    data\n    __typename\n  }\n}"
}
```
</dl>
